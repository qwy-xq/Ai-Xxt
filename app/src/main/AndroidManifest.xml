<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         权限说明
         ============================================================ -->

    <!-- OkHttp 联网拉 AI 接口必须 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ 通知权限，缺了 POST_NOTIFICATIONS 通知会被静默丢弃 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 前台服务权限（Android 9+ 必需） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 14+：每个 foregroundServiceType 都要单独声明对应权限
         这里选 specialUse，对应的运行时权限。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 可选：保持 CPU 在截屏/上传期间不休眠，避免后台被 Doze 杀掉 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 可选：开机自启（如果你希望 Service 跨重启存活） -->
    <!--
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiXxt">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================
             后台常驻 Service
             广播接收（ACTION_TRIGGER / ACTION_CONFIG）走 Service 内部
             registerReceiver 的匿名 BroadcastReceiver，不在 manifest 暴露。
             ============================================================ -->
        <service
            android:name=".BackgroundAssistService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Android 14+ 强制要求 specialUse 类型必须附 <property> 说明用途 -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="silent_screen_capture_and_ai_inference" />
        </service>

    </application>

</manifest>